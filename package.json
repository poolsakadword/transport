{
  "name": "delivery-routes-webapp",
  "version": "1.0.0",
  "description": "Delivery Route Management WebApp with Cloudflare Pages and D1 Database",
  "main": "_worker.js",
  "scripts": {
    "dev": "wrangler pages dev . --d1=DB=routes_db",
    "deploy": "wrangler pages deploy . --project-name=delivery-routes-webapp",
    "db:init:local": "wrangler d1 execute routes_db --local --file=./schema.sql && wrangler d1 execute routes_db --local --file=./seed.sql",
    "db:init:remote": "wrangler d1 execute routes_db --remote --file=./schema.sql && wrangler d1 execute routes_db --remote --file=./seed.sql"
  },
  "keywords": [
    "cloudflare-pages",
    "cloudflare-d1",
    "delivery-routes",
    "serverless"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "wrangler": "^3.109.2"
  }
}
